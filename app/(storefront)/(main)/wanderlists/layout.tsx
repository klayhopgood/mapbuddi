export const metadata = {
  title: "WanderLists - MapBuddi",
  description: "Browse curated WanderLists from local experts and travelers. Find the perfect places to visit and sync them to Google Maps.",
};

export default function WanderListsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
