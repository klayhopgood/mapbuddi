export const metadata = {
  title: "WanderList Details - MapBuddi",
  description: "Explore this curated WanderList with local insights and hidden gems. Purchase and sync directly to Google Maps.",
};

export default function WanderListLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
